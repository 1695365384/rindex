<p align="center">
  <img src="https://img.shields.io/badge/language-Rust-000000?style=for-the-badge&logo=rust" />
  <img src="https://img.shields.io/badge/license-MIT-8B5CF6?style=for-the-badge" />
  <img src="https://img.shields.io/badge/inference-CPU--only-10B981?style=for-the-badge" />
</p>

# rindex

[English](README.md) | [简体中文](README_zh.md)

> 本地代码索引与语义搜索引擎，为 LLM 编程智能体打造。
> 别再 grep 了。让你的 AI 真正理解代码。
> 为你的钱包保驾护航。

**rindex** 在本地为整个项目构建代码索引，通过 MCP（Model Context Protocol）提供语义搜索。让 Claude Code 用 **1-2 次工具调用**完成原来需要 5-7 次 grep + 逐文件阅读才能找到的代码。

---

## 核心价值

### 上下文窗口和调用次数预算，都是最昂贵的资源

Claude Code 的上下文窗口是 200K token。看起来很多，但一次 grep 往往会带来约 3,000 token 的噪音。在真实开发中，每天只搜索 20 次通常偏保守，很多团队会达到 50 次，甚至 100 次。

rindex 把单次搜索输出从约 3,000 token 压缩到约 70 token，同时把搜索工作流的调用次数从 5-7 次降到 1-2 次。**省下来的不只是 token，还有按调用次数计费的实际成本。**

### 算一笔账（按每日 20 / 50 / 100 次搜索）

| | Grep/Glob | rindex | 节省 |
|:---|---:|---:|---:|
| **每次搜索调用次数** | 5 – 7 次 | 1 – 2 次 | **调用数减少 ~70%** |
| **每次搜索响应 token** | ~3,000 | ~70 | **token 减少 ~98%** |
| **每日 20 次搜索（轻度）** | ~60,000 tok / 140 次调用 | ~1,400 tok / 40 次调用 | **58,600 tok + 100 次调用** |
| **每日 50 次搜索（常见开发）** | ~150,000 tok / 350 次调用 | ~3,500 tok / 100 次调用 | **146,500 tok + 250 次调用** |
| **每日 100 次搜索（重度）** | ~300,000 tok / 700 次调用 | ~7,000 tok / 200 次调用 | **293,000 tok + 500 次调用** |
| **等待时间（每次搜索）** | 5 – 7 轮往返 | 1 – 2 轮 | **响应快 3-5 倍** |

### 折算成多少钱（按 Claude Opus 的 token 单价）

假设：输入 token 单价为 **$15 / 1M tokens**。

| 工作负载 | 每日节省 token | 每天节省金额 | 每月节省金额（22 个工作日） | 每年节省金额 |
|:---|---:|---:|---:|---:|
| **20 次搜索/天** | 58,600 tok | **$0.88/天** | **$19.34/月** | **$232/年** |
| **50 次搜索/天** | 146,500 tok | **$2.20/天** | **$48.35/月** | **$580/年** |
| **100 次搜索/天** | 293,000 tok | **$4.40/天** | **$96.69/月** | **$1,160/年** |

| 团队场景 | 每月节省 token | 每月节省金额 | 每年节省金额 |
|:---|---:|---:|---:|
| **5 人团队，每人 50 次搜索/天** | 16.12M tok | **$241.75/月** | **$2,901/年** |
| **20 人团队，每人 100 次搜索/天** | 129.0M tok | **$1,933.80/月** | **$23,206/年** |

### 如果你的套餐按调用次数计费

除了 token，调用次数本身也能直接省钱：

| 工作负载 | 每天少多少次调用 | 每月少多少次调用（22 天） |
|:---|---:|---:|
| **20 次搜索/天** | 100 | 2,200 |
| **50 次搜索/天** | 250 | 5,500 |
| **100 次搜索/天** | 500 | 11,000 |

公式：**每月调用成本节省 = 每月节省调用次数 × 每次调用单价**。

示例（若每次调用 **$0.002**）：

| 工作负载 | 调用计费带来的额外节省 |
|:---|---:|
| **20 次搜索/天** | **$4.40/月** |
| **50 次搜索/天** | **$11.00/月** |
| **100 次搜索/天** | **$22.00/月** |

> 总经济效应 = token 节省金额 + 调用计费节省金额。

### 性能基准

| 操作 | 延迟 |
|:---|---:|
| 符号搜索 (1,000 chunks) | **1.7 µs** |
| 解析 500 行 Rust 文件 | **1.36 ms** |
| 扫描 38 文件项目 | **836 µs** |

### 一句话总结

> rindex 不是“更好的 grep”。它改变了 LLM 编程智能体的工作方式：
> **从"在噪音里翻找"变成"直接拿到答案"**。省下的 token 和时间，都变成更好的代码。

---

## 功能

- **语义检索** — 按代码含义搜索，而不只是做字符串匹配。搜索“数据库连接配置”也能命中 `DBPool::new()`。
- **符号定位** — 以微秒级速度定位函数、结构体、特征和类；相比传统文本检索可快约 1000 倍。
- **关联代码发现（find_related）** — 找到项目中语义相近的实现，一次调用可替代多轮检索拼凑。
- **自动建立索引** — 首次打开项目自动扫描，后续增量同步，基本零配置。
- **自定义忽略规则（.llm-index-ignore）** — 可配合 `.gitignore` 精细控制索引范围。
- **跨会话记忆** — 保存项目决策、踩坑记录和常用模式，下次对话自动注入，避免重复解释。
- **全本地运行** — 无需 API 密钥、无云端依赖、无遥测；静态词向量在 CPU 上即可运行，无需 GPU。

---

## 安装

### 一键安装（推荐）

下载发布包，解压，双击安装脚本：

| 平台 | 包名 | 安装方式 |
|---|---|---|
| **Windows 11** | `rindex-v1.0.0-windows-x64.zip` | 双击 `install.bat` |
| **macOS** | `rindex-v1.0.0-macos-arm64.tar.gz` | 双击 `install.command` |

二进制、嵌入模型、PATH 一步到位。装完后打开你的编码智能体进入任意项目，rindex 自动检测客户端并完成全部配置。零手动步骤。

**支持的客户端**: Claude Code、Cursor、Windsurf，以及任何兼容 MCP 协议的智能体。

### 从源码构建

```bash
cargo build --release && cp target/release/rindex ~/.local/bin/
rindex backfill  # 用静态模型为所有代码块生成向量（~166MB，发布包内含）
```

然后打开 Claude Code — rindex 自动处理其余一切。

---

## 客户端支持

任何兼容 MCP 的编码智能体均可使用。rindex 自动检测并配置：

| 客户端 | 配置文件 | 配置方式 |
|---|---|---|
| **Claude Code** | `.mcp.json` + `.claude/skills/` | 自动 |
| **Cursor** | `.cursor/mcp.json` | 自动 |
| **Windsurf** | `.windsurf/mcp.json` | 自动 |
| **其他 MCP 客户端** | 标准 MCP JSON 配置 | 指向 `rindex` |

所有客户端共享同一索引数据库 — 随时切换，无需重建索引。

## 可用工具

| 工具名 | 功能说明 |
|:---|:---|
| `search` | 对已建立索引的代码进行语义与关键词混合检索 |
| `search_symbol` | 按名称精确定位符号，如函数、类、类型、特征 |
| `find_related` | 查找与指定符号或文件位置语义接近的代码 |
| `project_status` | 查看索引进度、文件数量、代码块数量与模型状态 |
| `reindex` | 从头重建整个项目索引（后台执行） |
| `backfill` | 为缺失向量的代码块补齐向量 |
| `verify` | 校验索引完整性，并报告过期条目与缺失文件 |
| `session_note` | 保存可跨会话复用的项目记忆（如设计决策、踩坑记录、代码模式） |
| `session_context` | 读取历史会话中保存的项目记忆 |

---

## 工作原理

```
索引管线
────────────────────────────────────────────────────
  源文件   →  tree-sitter AST  →  函数/类级   →  静态嵌入表  →  SQLite
              解析符号            代码块          (查表,无框架)    存储
```

1. **扫描** — 文件遍历器根据 `.gitignore` + `.llm-index-ignore` 排除无关文件
2. **分块** — tree-sitter 将代码拆分为符号级代码块（函数、结构体、类）
3. **嵌入** — 自研蒸馏的静态 token 嵌入表（纯 CPU，~0.01ms/chunk — 就是查表，没有 transformer）
4. **搜索** — 查询向量化 → 余弦相似度重排序 → 紧凑文本输出

搜索采用**两阶段混合策略**：FTS5 全文预过滤（AND 语义）缩小候选集 → 嵌入相似度重排序。避免将所有向量加载到内存，支持 10 万+ 代码块的项目。

---

## 配置

### CLI

```
rindex [OPTIONS]

  -p, --path <PATH>       项目根目录 (默认: 当前目录)
      --db <PATH>         数据库路径 (默认: ~/Library/Application Support/rindex/rindex.db)
      --max-size <BYTES>  索引文件大小上限 (默认: 1 MB)
      --model-id <ID>     静态模型目录 (默认: c2llm-static-256)
      --no-model          跳过嵌入模型 — 仅文本搜索
```

### `rindex.toml`

```toml
max_file_size = 2097152           # 2 MB
model_id = "c2llm-static-256"    # model_cache_dir 下的本地目录
default_search_limit = 20
watcher_debounce_ms = 500
```

---

## 系统要求

- **Rust 1.75+**
- **不需要 GPU** — 纯 CPU 推理，任何机器都能跑
- **~170 MB** 磁盘用于模型缓存（token_embeddings.safetensors + tokenizer.json）
- **~50 MB** 磁盘每 1 万个索引块（SQLite + 向量数据）

---

## 许可证

MIT
